# ⚠️ Ограничения Vercel для этого проекта

## Проблема:

Vercel использует **read-only файловую систему** для serverless функций. Это означает, что **нельзя сохранять данные в файлы** (JSON файлы в папке `data/`).

## ✅ Что уже исправлено:

1. **Код обновлен** - добавлена проверка на Vercel окружение
2. **Запись данных отключена** в продакшене (Vercel)
3. **Чтение данных отключено** в продакшене (возвращаются пустые данные)
4. **API работает** - все endpoints отвечают успешно, но данные не сохраняются

## 📊 Что НЕ работает на Vercel:

- ❌ Сохранение посетителей (`visitors.json`)
- ❌ Сохранение пользователей (`users.json`)
- ❌ Сохранение розыгрышей (`giveaways.json`)
- ❌ Сохранение победителей (`winners.json`)
- ❌ Сохранение статистики поведения
- ❌ Управление пользователями (данные не сохраняются)

## ✅ Что РАБОТАЕТ на Vercel:

- ✅ Авторизация через Twitch
- ✅ Просмотр чата Twitch
- ✅ Создание розыгрышей (но не сохраняются)
- ✅ Определение победителей (но не сохраняются)
- ✅ Все функции интерфейса

## 🔧 Решения для продакшена:

### Вариант 1: Использовать другой хостинг (Рекомендуется)

**Railway** или **Render** - поддерживают запись файлов:

1. **Railway:**
   - https://railway.app
   - Бесплатный тариф
   - Поддерживает запись файлов
   - Автоматический деплой из GitHub

2. **Render:**
   - https://render.com
   - Бесплатный тариф
   - Поддерживает запись файлов
   - Простой интерфейс

### Вариант 2: Использовать базу данных

Для Vercel можно использовать:
- **Vercel KV** (Key-Value store)
- **MongoDB Atlas** (бесплатно)
- **Supabase** (бесплатно)
- **PlanetScale** (бесплатно)

Но это требует переписывания кода для работы с БД.

### Вариант 3: Использовать только для демо

Текущая версия работает для демонстрации функционала, но данные не сохраняются между сессиями.

## 🚀 Рекомендация:

**Используйте Railway или Render** для продакшена - они бесплатны и поддерживают запись файлов, как локальный сервер.

Инструкции по деплою на Railway/Render есть в файле `DEPLOY_GUIDE.md`.

