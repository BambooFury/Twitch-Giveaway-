# Как правильно запустить приложение

## ✅ Рекомендуемый способ (через Node.js сервер)

### Шаг 1: Запустите сервер
Откройте CMD/PowerShell в папке проекта и выполните:
```bash
npm start
```

Вы увидите:
```
🚀 Сервер запущен на порту 3000
📁 Данные сохраняются в: C:\Users\...\data
🌐 API доступен по адресу: http://localhost:3000/api
📄 Приложение доступно по адресу: http://localhost:3000/
```

### Шаг 2: Откройте в браузере
Откройте браузер и перейдите по адресу:
```
http://localhost:3000/
```

**Преимущества:**
- ✅ Все функции работают (IP адреса, регистрация посетителей)
- ✅ API доступен
- ✅ Данные сохраняются на сервере
- ✅ Один порт для всего

---

## 🔄 Альтернативный способ (Live Server + Node.js сервер)

Если вы используете Live Server в VS Code:

### Шаг 1: Запустите Node.js сервер (в CMD)
```bash
npm start
```

### Шаг 2: Откройте через Live Server
- Правой кнопкой на `ewropg.html` → "Open with Live Server"
- Live Server откроет на порту 5500 (или другом)
- Node.js сервер работает на порту 3000

**Важно:** В этом случае:
- Live Server раздает HTML (порт 5500)
- Node.js сервер обрабатывает API (порт 3000)
- В `ewropg.html` должно быть: `SERVER_URL = 'http://localhost:3000'`

---

## ❌ Не рекомендуется (только файл)

Если просто открыть `ewropg.html` через файловую систему:
- ❌ CORS ошибки при запросах к серверу
- ❌ IP адреса не будут работать
- ❌ Регистрация посетителей не будет работать

---

## 🔧 Настройка

В файле `ewropg.html` найдите:
```javascript
const SERVER_URL = 'http://localhost:3000';
const USE_SERVER = true; // Включить использование сервера
```

- Если `USE_SERVER = true` - нужен запущенный Node.js сервер
- Если `USE_SERVER = false` - сервер не нужен, но функции сервера не работают

---

## 📝 Резюме

**Лучший способ:**
1. `npm start` в CMD
2. Открыть `http://localhost:3000/` в браузере

**Сервер должен быть запущен постоянно**, пока вы используете приложение.

