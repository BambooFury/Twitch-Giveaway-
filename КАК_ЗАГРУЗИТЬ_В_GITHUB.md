# 📤 Как загрузить проект в GitHub репозиторий

## ⚠️ ВАЖНО: Сначала установите Git!

Если вы видите ошибку `git : Имя "git" не распознано`, значит Git не установлен.

**Смотрите файл `УСТАНОВКА_GIT.md`** для инструкций по установке Git.

Или используйте **Вариант 2** ниже - загрузка через браузер (без Git).

---

## 📋 Шаг 1: Создайте репозиторий на GitHub

1. Откройте https://github.com
2. Войдите в свой аккаунт
3. Нажмите **"+"** в правом верхнем углу → **"New repository"**
4. Заполните:
   - **Repository name:** `bamboo-fury-giveaway` (или любое другое имя)
   - **Description:** (опционально) "Twitch Giveaway Application"
   - **Visibility:** Public или Private (на ваше усмотрение)
   - **НЕ отмечайте** "Initialize this repository with a README"
5. Нажмите **"Create repository"**

---

## 🔄 Вариант 2: Загрузка через браузер (БЕЗ Git)

Если Git не установлен, можно загрузить файлы через веб-интерфейс:

1. На странице репозитория нажмите **"uploading an existing file"**
2. Перетащите файлы из папки проекта
3. **НЕ загружайте:**
   - ❌ Папку `node_modules/`
   - ❌ Файлы из `data/*.json`
4. Внизу страницы:
   - **Commit message:** "Initial commit"
   - Нажмите **"Commit changes"**

**Минус:** При каждом обновлении нужно загружать файлы заново через браузер.

---

## 📋 Шаг 2: Откройте PowerShell в папке проекта (Только если установлен Git)

1. Откройте папку проекта: `C:\Users\demon\OneDrive\BambooFury Giveaway`
2. Нажмите `Shift + ПКМ` (правая кнопка мыши) в пустом месте папки
3. Выберите **"Open PowerShell window here"** или **"Открыть окно PowerShell здесь"**

---

## 📋 Шаг 3: Инициализируйте Git (если еще не сделано)

Выполните в PowerShell:

```powershell
git init
```

---

## 📋 Шаг 4: Добавьте все файлы

```powershell
git add .
```

Это добавит все файлы проекта (кроме тех, что в `.gitignore`)

---

## 📋 Шаг 5: Создайте первый коммит

```powershell
git commit -m "Initial commit - Twitch Giveaway App"
```

---

## 📋 Шаг 6: Подключите GitHub репозиторий

Замените `ваш-username` и `ваш-репозиторий` на ваши данные:

```powershell
git remote add origin https://github.com/ваш-username/ваш-репозиторий.git
```

Например:
```powershell
git remote add origin https://github.com/bamboofury/bamboo-fury-giveaway.git
```

**Где найти URL:**
- Откройте ваш репозиторий на GitHub
- Нажмите зеленую кнопку **"Code"**
- Скопируйте URL (HTTPS)

---

## 📋 Шаг 7: Загрузите файлы на GitHub

```powershell
git branch -M main
git push -u origin main
```

Если GitHub попросит авторизацию:
- Введите ваш **username** (не email)
- Введите **Personal Access Token** (не пароль)

---

## 🔑 Как создать Personal Access Token:

Если GitHub просит токен вместо пароля:

1. Откройте https://github.com/settings/tokens
2. Нажмите **"Generate new token"** → **"Generate new token (classic)"**
3. Заполните:
   - **Note:** "Render Deploy"
   - **Expiration:** 90 days (или No expiration)
   - **Scopes:** Отметьте `repo` (все подпункты)
4. Нажмите **"Generate token"**
5. **Скопируйте токен** (он показывается только один раз!)
6. Используйте этот токен как пароль при `git push`

---

## ✅ Проверка:

1. Откройте ваш репозиторий на GitHub
2. Должны быть видны все файлы проекта
3. **НЕ должно быть:**
   - `node_modules/` (папка)
   - `data/*.json` (файлы данных)

---

## 🔄 Обновление кода в будущем:

После изменений в коде:

```powershell
git add .
git commit -m "Описание изменений"
git push
```

---

## ⚠️ Важно:

- **`.gitignore`** уже создан - он исключает `node_modules` и файлы данных
- **Не загружайте** файлы с паролями или токенами
- **Файлы данных** (`data/*.json`) не загружаются - они создадутся автоматически на сервере

---

## 🐛 Если возникли проблемы:

### Ошибка: "remote origin already exists"
```powershell
git remote remove origin
git remote add origin https://github.com/ваш-username/ваш-репозиторий.git
```

### Ошибка: "failed to push"
- Проверьте, что вы авторизованы в GitHub
- Используйте Personal Access Token вместо пароля

### Ошибка: "nothing to commit"
- Убедитесь, что вы сделали изменения в файлах
- Или используйте `git add -A` вместо `git add .`

