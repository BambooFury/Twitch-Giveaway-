# Быстрый старт

## ⚠️ ВАЖНО: Используйте ТОЛЬКО один способ!

### ✅ Способ 1: Через Node.js сервер (РЕКОМЕНДУЕТСЯ)

1. **Запустите сервер в CMD:**
   ```bash
   npm start
   ```

2. **Откройте в браузере:**
   ```
   http://localhost:3000/
   ```

3. **НЕ запускайте Live Server!** Закройте его, если он открыт.

**Преимущества:**
- ✅ Все функции работают (IP адреса, регистрация посетителей)
- ✅ API доступен
- ✅ Данные сохраняются на сервере
- ✅ Нет конфликтов портов

---

### ❌ Способ 2: Через Live Server (НЕ РЕКОМЕНДУЕТСЯ)

Если вы используете Live Server:

1. **Отключите сервер в коде:**
   В файле `ewropg.html` найдите и измените:
   ```javascript
   const USE_SERVER = false; // Отключить сервер
   ```

2. **Откройте через Live Server:**
   - Правой кнопкой на `ewropg.html` → "Open with Live Server"

3. **НЕ запускайте `npm start`!**

**Недостатки:**
- ❌ IP адреса не будут работать
- ❌ Регистрация посетителей не будет работать
- ❌ Данные не сохраняются на сервере

---

## 🔧 Решение проблемы с перезагрузкой

Если страница постоянно перезагружается:

1. **Убедитесь, что запущен ТОЛЬКО один сервер:**
   - Либо `npm start` (Node.js)
   - Либо Live Server
   - НЕ оба одновременно!

2. **Если используете Node.js сервер:**
   - Откройте `http://localhost:3000/`
   - НЕ открывайте через Live Server

3. **Если используете Live Server:**
   - Установите `USE_SERVER = false` в коде
   - НЕ запускайте `npm start`

---

## 📝 Проверка

После запуска `npm start` вы должны увидеть:
```
🚀 Сервер запущен на порту 3000
📁 Данные сохраняются в: ...
🌐 API доступен по адресу: http://localhost:3000/api
📄 Приложение доступно по адресу: http://localhost:3000/

✅ Откройте в браузере: http://localhost:3000/
⚠️  НЕ используйте Live Server одновременно!
```

Откройте `http://localhost:3000/` в браузере - страница должна загрузиться без перезагрузок.

