# 🚀 Руководство по деплою сайта

## Варианты размещения сайта в интернете

### 1. **Быстрый способ (для тестирования) - ngrok**

**Для временного доступа (бесплатно):**

1. Скачайте ngrok: https://ngrok.com/download
2. Запустите ваш сервер локально:
   ```bash
   node server.js
   ```
3. В новом терминале запустите ngrok:
   ```bash
   ngrok http 3000
   ```
4. Скопируйте URL (например: `https://abc123.ngrok.io`) и поделитесь им

**⚠️ Минусы:** URL меняется при каждом запуске (в бесплатной версии)

---

### 2. **Vercel (Рекомендуется - бесплатно)**

**Для постоянного размещения:**

1. Установите Vercel CLI:
   ```bash
   npm install -g vercel
   ```

2. В папке проекта выполните:
   ```bash
   vercel
   ```

3. Следуйте инструкциям в терминале

4. После деплоя получите постоянный URL: `https://ваш-проект.vercel.app`

**✅ Плюсы:** Бесплатно, автоматический HTTPS, простой деплой

---

### 3. **Railway (Бесплатно с ограничениями)**

1. Зарегистрируйтесь на https://railway.app
2. Создайте новый проект
3. Подключите GitHub репозиторий (или загрузите файлы)
4. Railway автоматически определит Node.js проект
5. Добавьте переменную окружения `PORT` (Railway установит автоматически)

**✅ Плюсы:** Бесплатный тариф, автоматический деплой

---

### 4. **Render (Бесплатно)**

1. Зарегистрируйтесь на https://render.com
2. Создайте новый "Web Service"
3. Подключите GitHub репозиторий
4. Укажите:
   - **Build Command:** `npm install`
   - **Start Command:** `node server.js`
   - **Environment:** `Node`

**✅ Плюсы:** Бесплатный тариф, простой интерфейс

---

### 5. **Heroku (Платно, но есть бесплатные альтернативы)**

1. Установите Heroku CLI
2. Создайте файл `Procfile` в корне проекта:
   ```
   web: node server.js
   ```
3. Выполните:
   ```bash
   heroku create
   git push heroku main
   ```

---

## 📝 Что нужно сделать перед деплоем:

### 1. Создайте `.gitignore` (если используете Git):
```
node_modules/
data/*.json
.env
.DS_Store
```

### 2. Обновите `ewropg.html`:
Найдите строку с `SERVER_URL` и замените на URL вашего деплоя:
```javascript
const SERVER_URL = 'https://ваш-домен.com'; // или оставьте пустым для автоопределения
```

### 3. Для Vercel создайте `vercel.json`:
```json
{
  "version": 2,
  "builds": [
    {
      "src": "server.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "server.js"
    }
  ]
}
```

### 4. Для Render создайте `render.yaml`:
```yaml
services:
  - type: web
    name: twitch-giveaway
    env: node
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
```

---

## 🔧 Настройка для продакшена:

### 1. Обновите CORS в `server.js` (если нужно):
Замените `origin: '*'` на ваш домен:
```javascript
app.use(cors({
    origin: 'https://ваш-домен.com',
    // ...
}));
```

### 2. Добавьте переменные окружения:
- `PORT` - порт (обычно устанавливается автоматически)
- `NODE_ENV=production`

---

## 🌐 После деплоя:

1. **Проверьте работу сайта** по полученному URL
2. **Поделитесь ссылкой** с другими пользователями
3. **Настройте домен** (опционально) - можно подключить свой домен

---

## ⚡ Самый быстрый способ (5 минут):

1. Установите Vercel CLI: `npm install -g vercel`
2. В папке проекта: `vercel`
3. Следуйте инструкциям
4. Готово! Получите URL и делитесь им

---

## 📞 Нужна помощь?

Если возникли проблемы:
- Проверьте логи деплоя
- Убедитесь, что все зависимости установлены
- Проверьте, что порт настроен правильно

