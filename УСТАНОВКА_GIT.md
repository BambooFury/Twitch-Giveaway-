# 🔧 Установка Git для Windows

## Вариант 1: Установить Git (Рекомендуется)

### Шаг 1: Скачайте Git

1. Откройте https://git-scm.com/download/win
2. Скачайте установщик (автоматически определит 64-bit или 32-bit)
3. Запустите установщик

### Шаг 2: Установка

1. Нажимайте **"Next"** на всех шагах
2. **Важно:** На шаге "Choosing the default editor" выберите:
   - **"Use Visual Studio Code as Git's default editor"** (если у вас установлен VS Code)
   - Или **"Nano editor"** (простой вариант)
3. На шаге "Adjusting your PATH environment":
   - Выберите **"Git from the command line and also from 3rd-party software"**
4. Остальные настройки оставьте по умолчанию
5. Нажмите **"Install"**

### Шаг 3: Проверка установки

1. **Закройте** текущее окно PowerShell
2. Откройте **новое** окно PowerShell
3. Выполните:
   ```powershell
   git --version
   ```
4. Должно показать версию Git (например: `git version 2.42.0`)

### Шаг 4: Настройка Git (первый раз)

```powershell
git config --global user.name "Ваше Имя"
git config --global user.email "ваш-email@example.com"
```

---

## Вариант 2: Загрузить через веб-интерфейс GitHub (Без Git)

Если не хотите устанавливать Git, можно загрузить файлы через браузер:

### Шаг 1: Создайте репозиторий на GitHub

1. Откройте https://github.com
2. Нажмите **"+"** → **"New repository"**
3. Название: `bamboo-fury-giveaway`
4. **НЕ отмечайте** "Initialize this repository with a README"
5. Нажмите **"Create repository"**

### Шаг 2: Загрузите файлы через браузер

1. На странице репозитория нажмите **"uploading an existing file"**
2. Перетащите все файлы из папки проекта (кроме `node_modules` и `data/*.json`)
3. Внизу страницы:
   - **Commit message:** "Initial commit"
   - Нажмите **"Commit changes"**

### ⚠️ Важно при загрузке через браузер:

**НЕ загружайте:**
- ❌ Папку `node_modules/` (слишком большая)
- ❌ Файлы из `data/*.json` (они создадутся на сервере)
- ❌ Папку `.vercel/` (если есть)

**Загрузите:**
- ✅ `ewropg.html`
- ✅ `server.js`
- ✅ `package.json`
- ✅ `render.yaml`
- ✅ Все `.md` файлы
- ✅ `favicon.svg`
- ✅ `.gitignore`

---

## 🎯 Рекомендация:

**Установите Git** (Вариант 1) - это займет 5 минут, но потом будет проще обновлять код.

После установки Git следуйте инструкции из файла `КАК_ЗАГРУЗИТЬ_В_GITHUB.md`.

